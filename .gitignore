__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

.idea/
.vscode/
*.swp
*.swo
*~

# ==================== Logs ====================
# 日志文件 - dota_helper/logs/ 目录
# 结构: logs/YYYY-MM-DD/part-N/*.log
dota_helper/logs/
*.log
*.json.log

# ==================== Cache ====================
# 缓存数据库文件 - dota_helper/cache/cache.db
dota_helper/cache/*.db

# API数据缓存文件 - 英雄克制数据等
# 这些文件可以通过API重新获取，不需要提交到git
dota_helper/cache/hero_matchups_*.json
dota_helper/cache/heroes_list.json

# Matchup 数据目录 - 英雄对局数据本地存储
dota_helper/data/matchups/

# 通用数据库文件
*.db
*.sqlite
*.sqlite3

# ==================== Knowledge Management ====================
# 向量数据库存储目录 - Chroma 持久化数据
dota_helper/data/chroma_db/

# 本地 Embedding 模型缓存
.cache/
*.cache/

# 知识库临时文件
*.index
*.faiss

# ==================== Runtime Data ====================
# 运行时数据目录
memory_runtime/
cache_runtime/

# ==================== Test Data ====================
# 测试数据目录
test_memory/
test_prefix/
dota_helper/test_cache/

# OS files
.DS_Store
Thumbs.db

# Output projects
out_project/

# Trae IDE
.trae/

# Superpowers
.superpowers/

# ==================== Local Docs (not for GitHub) ====================
# 个人/过程性文档 - 仅本地保留，不上传到 GitHub
dota_helper/docs/
# 根目录设计文档（架构/计划/issue_designs） - 仅本地保留，不上传到 GitHub
doc/

# ==================== Post Match Review Reports ====================
# 复盘报告输出目录 - 包含生成的 Markdown 报告，不上传到 git
dota_helper/reports/

# ==================== Competitor Agent Reports ====================
# 竞品分析报告输出目录 - CLI/CI 生成的 Markdown/CSV 产物，不上传到 git
competitor_agent/reports/
# 仓库根目录测试产物 reports/（测试运行时的落盘输出）
reports/
# web_app 运行时错误日志（临时产物）
competitor_agent/web.err

# ==================== dota_helper (no longer synced) ====================
# dota_helper 子项目不再同步到远程仓库，整目录本地保留
dota_helper/

# ==================== External Data (out of repo) ====================
# 工具缓存（pytest/ruff/mypy）已固定到 ~/.cache/competitor_agent（见 competitor_agent/pyproject.toml）
# 报告生成物已固定到 ~/.competitor_agent/reports（见 config/review_config.yaml / secret_vault.get_reports_dir）
# 以下条目仅作安全兜底：当以仓库根为 CWD 运行、或显式用相对路径覆盖时仍可忽略
