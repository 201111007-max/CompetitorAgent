# DotaHelperAgent 核心依赖

# Web 框架
flask>=2.0.0
flask-cors>=3.0.0

# HTTP 请求
requests>=2.28.0
httpx>=0.24.0

# 配置管理
pyyaml>=6.0

# 数据处理
python-dateutil>=2.8.0

# MCP Server 核心
mcp>=1.0.0
python-dotenv>=1.0.0

# 数据分析（MCP Server 眼位分析/RAG）
pandas>=1.5.0
numpy>=1.23.0

# 可视化（MCP Server 眼位热力图/散点图）
matplotlib>=3.5.0
pillow>=9.0.0

# 可选依赖 - Langfuse 监控（推荐安装）
# 安装命令: pip install langfuse
# 或: pip install -r requirements-optional.txt
# langfuse>=2.0.0

# Knowledge Management System
chromadb>=0.5.0
openai>=1.12.0
sentence-transformers>=2.2.2

# 可选依赖 - 语音提醒系统（推荐安装）
# 安装命令: pip install pygame
# 或: pip install -r requirements-optional.txt
# pygame>=2.0.0

# 评测系统依赖（推荐安装）
# 安装命令: pip install pytest pytest-asyncio scipy
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# scipy>=1.10.0

# 可选依赖 - 高级评估工具
# 安装命令: pip install deepeval mlflow
# deepeval>=0.20.0
# mlflow>=2.10.0

# 可选依赖 - MCP Server 增强功能
# 眼位热力图增强: pip install opencv-python
# opencv-python>=4.6.0
# RAG 向量检索加速: pip install faiss-cpu
# faiss-cpu>=1.7.0
