# dota_helper - 开发与测试依赖
# 安装: pip install -r requirements.txt -r requirements-dev.txt

# ── 测试 ──
pytest>=8.0
pytest-asyncio>=0.23.0
respx>=0.23.0            # httpx mock 库

# ── 代码质量 ──
ruff>=0.1.0              # Linter + Formatter
mypy>=1.0                # 类型检查

# ── 覆盖率 ──
pytest-cov>=4.0
